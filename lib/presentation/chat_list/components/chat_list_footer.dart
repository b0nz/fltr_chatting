import 'package:flutter/material.dart';

class ChatListFooter extends StatelessWidget {
  const ChatListFooter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
